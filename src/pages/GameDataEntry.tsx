import './GameDataEntry.css';
import MainTemplate from "../components/MainTemplate";

// Temporary name.  Change to something more appropriate when it's clear what that would be.
const GameDataEntry: React.FC = () => {
    return (
        <MainTemplate name="game-data-entry" nameDisplay="录入游戏数据">
            录入游戏数据表单 placeholder
        </MainTemplate>
    );
};

export default GameDataEntry;

