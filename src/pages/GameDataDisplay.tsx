import './GameDataDisplay.css';
import MainTemplate from "../components/MainTemplate";

// Temporary name.  Change to something more appropriate when it's clear what that would be.
const GameDataDisplay: React.FC = () => {
    return (
        <MainTemplate name="game-data-display" nameDisplay="输出游戏数据">
            输出游戏数据表单 placeholder
        </MainTemplate>
    );
};

export default GameDataDisplay;

