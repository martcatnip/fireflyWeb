import { MainLayout } from "../layouts/MainLayout";

// Temporary name.  Change to something more appropriate when it's clear what that would be.
export const IndividualProgress: React.FC = () => {
    return (
        <MainLayout pageName="individual-progress" pageNameDisplay="学生个人进展">
            学生个人进展表单 placeholder
        </MainLayout>
    );
};
