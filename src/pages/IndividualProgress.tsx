import './IndividualProgress.css';
import MainTemplate from "../components/MainTemplate";

// Temporary name.  Change to something more appropriate when it's clear what that would be.
const IndividualProgress: React.FC = () => {
    return (
        <MainTemplate name="individual-progress" nameDisplay="学生个人进展">
            学生个人进展表单 placeholder
        </MainTemplate>
    );
};

export default IndividualProgress;

