import './ReadingDataEntry.css';
import MainTemplate from "../components/MainTemplate";

// Temporary name.  Change to something more appropriate when it's clear what that would be.
const ReadingDataEntry: React.FC = () => {
    return (
        <MainTemplate name="reading-data-entry" nameDisplay="录入朗读数据">
            录入朗读数据表单 placeholder
        </MainTemplate>
    );
};

export default ReadingDataEntry;

